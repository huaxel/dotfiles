# Dotter configuration
[default]

[default.files]
# Core dotfiles
# Keep the source name outside npm's project-config lookup; it may contain
# registry credentials and is gitignored.
npmrc = "~/.npmrc"

# llama.cpp models.ini — template because model paths are machine-specific
"llama-models.ini" = "~/.config/llama.cpp/models.ini"

[windows.files]
# Unix SSH configuration is Home Manager-owned; Windows remains Dotter-managed.
ssh_config = "~\\.ssh\\config"
# Git configuration remains Dotter-managed on Windows.
gitconfig = "~\\.gitconfig"
# Git ignore file remains Dotter-managed on Windows.
gitignore_global = "~\\.gitignore_global"
# mise and Herdr remain Dotter-managed on Windows.
"config/mise" = "~\\.config\\mise"
"config/herdr" = "~\\.config\\herdr"

# Zed
"config/zed/keymap.json" = { target = "$LOCALAPPDATA\\Zed\\keymap.json", type = "symbolic" }

# PowerShell 7 profile
"powershell/7/profile.ps1" = { target = "~\\Documents\\PowerShell\\Microsoft.PowerShell_profile.ps1", type = "symbolic" }

# PowerShell 5.1 profile
"powershell/5.1/profile.ps1" = { target = "~\\Documents\\WindowsPowerShell\\Microsoft.PowerShell_profile.ps1", type = "symbolic" }

# Windows Terminal
"windows-terminal/settings.json" = { target = "$LOCALAPPDATA\\Packages\\Microsoft.WindowsTerminal_8wekyb3d8bbwe\\LocalState\\settings.json", type = "symbolic" }

# AutoHotkey — Mac-like key remap
"autohotkey/mac-layout.ahk" = { target = "~\\.config\\autohotkey\\mac-layout.ahk", type = "symbolic" }

# Flow Launcher
"flow-launcher/README.md" = { target = "~\\.config\\flow-launcher\\README.md", type = "symbolic" }

# GlazeWM — Tiling window manager
"glazewm/config.yaml" = { target = "~\\.glzr\\glazewm\\config.yaml", type = "symbolic" }

# Zebar — Status bar for GlazeWM
"zebar/settings.json" = { target = "~\\.glzr\\zebar\\settings.json", type = "symbolic" }

# Neovim
"config/nvim" = "~\\AppData\\Local\\nvim"

# llama.cpp Windows launcher
"config/llama.cpp/start-server.ps1" = "~\\.config\\llama.cpp\\start-server.ps1"
"config/llama.cpp/README-windows.md" = "~\\.config\\llama.cpp\\README.md"

# Bat — cat replacement with syntax highlighting
"config/bat" = "~\\.config\\bat"

# Btop — resource monitor
"config/btop" = "~\\.config\\btop"

# Eza — modern ls replacement
"config/eza" = "~\\.config\\eza"

# Nushell — Nushell uses %APPDATA%\nushell on Windows
"config/nushell" = { target = "~\\AppData\\Roaming\\nushell", type = "symbolic" }

# Fastfetch — system info
"config/fastfetch" = "~\\.config\\fastfetch"

[unix.files]
# macOS + Linux shared configs. OS-specific dirs live in config-linux/ and
# config-macos/ (mapped in [linux.files]/[macos.files]) so they don't deploy
# on the wrong OS. Keep each root explicit so migrated roots can be removed
# without leaving Dotter and Home Manager managing the same path.
"config/htop" = "~/.config/htop"
# config/gh and config/qBittorrent were previously listed here but those apps
# self-manage their config dirs (gh writes config.yml/hosts.yml on login;
# qBittorrent creates its own dir on first run) and the empty source dirs are
# not tracked by git, breaking `dotter deploy` on fresh clones. Removed.

# llama.cpp mcp.json is auto-generated by sync-mcp-configs.py

# ─────────────────────────────────────────────────────────────────
# macOS-only configs. Enable with the "macos" package in local.toml.
# The former [linux.files] package was removed: all Linux configs now
# live in Home Manager (home/linux-desktop.nix), so there is no
# config-linux deployment via Dotter anymore.
# ─────────────────────────────────────────────────────────────────
[macos.files]
aerospace = "~/.aerospace.toml"
"config-macos/borders" = "~/.config/borders"

[windows.variables]
name = "Juan Benjumea"
email = "benjumeamoreno@gmail.com"
github_username = "huaxel"
hostname_color = "fg:#f7768e"
