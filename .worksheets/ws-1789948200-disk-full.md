# Disk getting full — 2026-09-21

## Task
Investigate why disk space appears to be getting full.

## Human notes

## Todos
- [x] Inspect filesystem, inode, and large-directory usage
- [x] Identify the main growth source and safe cleanup options

## Progress
- Started disk-capacity investigation.
- Confirmed Linux root has 882 GB free; the pressure is Windows C: at 100% (about 3.6 GB free).
- Measured Linux cache candidates and confirmed no cleanup was performed.

## Findings

## Decisions

## Questions / Next steps

## Progress
- Confirmed Linux root filesystem is healthy: 1.0 TB total, 73 GB used, 884 GB free (8%); inode usage 4%.
- Confirmed the apparent full disk is the Windows-mounted C: drive: 444 GB total, 438 GB used, 6.4 GB free (99%).
- Linux-side largest consumers are caches: `~/.cache/cpython` ~16 GB, `~/.cache/uv` ~3.8 GB, `~/.cache/qmd` ~2.2 GB, `~/.cache/ms-playwright` ~1.3 GB; `~/.cache` totals ~26 GB.
- Additional Linux usage: project-atom ~7.1 GB (including ~1.9 GB `.git` and ~4.6 GB `python`), npm ~2.4 GB, local share ~2.9 GB, logs ~1 GB.
- Docker has ~1.6 GB reclaimable, but it is on the Linux filesystem, not the C: pressure shown above.

## Findings
- The alert is about Windows C:, not an exhausted Linux filesystem. WSL's Linux filesystem is stored in a Windows virtual disk, so Linux usage still contributes to C: even though `df` inside WSL reports free space.
- The largest identified reclaimable candidate is `~/.cache/cpython` (~16 GB), followed by uv (~3.8 GB), qmd models (~2.2 GB), Playwright (~1.3 GB), npm cache (~1.8 GB), and Docker (~1.6 GB reclaimable). These are caches, not source data.
- Windows-side accounting is partially blocked by Windows ACLs from WSL; protected files (`pagefile.sys`, `hiberfil.sys`, etc.) and other users' profiles cannot be sized reliably here.

## Questions / Next steps
- Safe cleanup can reclaim cache space, but no deletion was performed. Windows Storage settings/TreeSize/WinDirStat run on Windows is needed to account for the remaining C: usage, especially protected system files and user profiles.

## Progress
- User authorized cleanup of the Python cache, UV cache, and Docker artifacts.
- Removed `~/.cache/cpython` (~16 GB).
- `uv cache clean` reported no cache at its configured path (the earlier UV estimate was not present now).
- Ran `docker system prune --all --volumes --force`; reclaimed 1.564 GB.

## Findings
- Linux filesystem remains healthy after cleanup; Windows C: pressure is reduced indirectly because WSL storage and Docker data are backed by Windows storage.

## Questions / Next steps
- Recheck Windows C: free space. No further deletion is planned without authorization.

## Progress
- User authorized cleanup. Verified no active Python/uv jobs.
- Cleared `~/.cache/cpython` (about 16 GB) and ran `uv cache clean` (7.9 GB reported removed); removed residual empty cache directories.
- Linux filesystem usage fell from 73 GB to 56 GB used; free space increased to 901 GB (6%). Remaining caches: CPython 4 KB, uv 15 MB.

## Progress
- Inspected Windows C: using PowerShell from WSL.
- Found the dominant consumer: `C:\Users\jbenjumeamoreno\AppData\Local\wsl\archlinux\ext4.vhdx` is 163.32 GB.
- Other major Windows consumers are `atom-data` (~100 GB), Scoop apps/cache (~49.8 GB), and Windows `AppData\Local\Microsoft` (~8.1 GB).

## Findings
- The remaining C: pressure is primarily the Arch WSL virtual disk, not ordinary Windows temp files. Linux reports only ~56 GB used, so the VHDX has reclaimable sparse/free blocks that need compaction rather than file deletion.
- `atom-data` is important project data and should not be deleted or compacted blindly.

## Questions / Next steps
- Safe next operation: run `fstrim` in Linux, shut down WSL, then compact `ext4.vhdx` from Windows. This requires stopping all WSL processes; no compaction has been performed yet.

## Progress
- User authorized Scoop cleanup. Ran `scoop cleanup *`; it removed many obsolete app versions but stopped on a stale ChatGPT directory path that no longer existed.
- Ran `scoop cache rm *` successfully: removed 89 installer archives and 6.2 GB.
- No manual deletion of the remaining Scoop app directories was performed because the cleanup command encountered the stale path.
