# Commit Sweep: 2026-08-09

Commits reviewed: 789569c..5316e61 (3 commits, restart/bug-fix batch)

## Commits reviewed

- `32b0662` fix(restart): don't abort in-flight turn, per-session guard, native timeout
- `27c191a` fix(restart): skip editor review when restarting
- `5316e61` chore(pi): drop unused pi-intercom from extension settings

## Issues found

### Commit hygiene
- ✅ All three messages are conventional (`fix(restart):`, `chore(pi):`) and descriptive.
- ✅ No fixup!/wip commits, no binaries, no secrets (checked — sops gate clean).
- ✅ External edit (`5316e61`, pi-intercom removal) was surfaced to the user and committed deliberately, not silently.

### Doc drift
- ✅ restart.ts header comment updated in the same commit as the behavior change (27c191a) — no stale docs.
- ℹ️ AGENTS.md pi section still describes the pre-PI_CODING_AGENT_DIR world in places (mentions both `~/dotfiles/pi/agent/extensions` AND `~/.pi/agent/extensions` as live trees needing sync). Investigation this session confirmed only the dotfiles tree is live (extensions symlink, settings env-var driven). AGENTS.md says to keep `~/.pi/agent/extensions` in sync — recommendation: note that the runtime tree is the dotfiles tree itself (symlink), so the sync caveat is stale. Not blocking; doc-only.

### Test quality
- ✅ No functional code added without the relevant path being exercised: restart.ts was re-validated (`just check-ts` syntax gate + runtime byte-identical + this session's load check). No unit-test infrastructure exists for extensions in this repo (gate is lint/syntax), consistent with the codebase pattern (same as herdr-fork, footer, etc.).
- ✅ pi-sync-extensions symlink no-op verified both paths (symlink → no-op, real dir → copy) as noted in commit.

### Cross-commit patterns
- ✅ Consistent: `guardOpen` Set cleanups added on the only lifecycle hook that exists (`session_shutdown`), matching existing `lastPct` cleanup.
- ✅ One root cause per fix; no duplicated logic introduced.

### Architecture drift
- ✅ No new dependencies. In fact `5316e61` REMOVES an unused one (`npm:pi-intercom`), verified absent from `pi/agent/npm/node_modules/`.
- ✅ restart.ts stays within extension API surface (`pi.on`, `ctx.ui`, `ctx.newSession`, `ctx.waitForIdle`) — no layering violations.

## Self-review findings (reviewer agent failed twice — exit 1 — so reviewed in-session)

### Minor (deferred, no fix this batch — scope is bug-fixes-only)
1. **Whitespace handoff regression:** `32b0662` guarded the *edited* value with `!edited.trim()`; `27c191a` removed the editor and the generated path now guards only `if (!prompt)`. A whitespace-only generated prompt would not be caught. Near-zero probability (LLM-summarized prompt), but it slightly regresses the documented intent. Suggested one-liner if the user wants it: `if (!prompt || !prompt.trim()) return ctx.ui.notify("Handoff cancelled", "info");`
2. **No feedback while waiting:** `if (!ctx.isIdle()) await ctx.waitForIdle()` can block /restart behind a long in-flight generation with no UI indication — user might think it hung. Suggested: `ctx.ui.notify("Waiting for current turn to settle…", "info")` before the wait. Not a hang: waitForIdle resolves once the run settles.

### Verified clean
- No race in guardOpen: `finally` deletes before the session-id-switch check; `session_shutdown` cleans both maps; multi-session isolation works.
- Native `{ timeout: AFK_MS }` replaces AbortController correctly (countdown + auto-dismiss).
- pi-sync-extensions: symlink → `cd` follows → `pwd -P` matches → no-op; real dir → copy; missing/dangling handled. Solid.

## Conclusion

No Critical or Important issues. Two Minor code findings (whitespace guard, wait feedback) + two Minor doc-drift notes (AGENTS.md stale sync caveat — the runtime tree IS the dotfiles tree via symlink, so the "keep in sync" note is stale; `intercom/` empty leftover dirs in both agent trees — harmless). Batch is clean, conventional, and fully verified: `just ci` green, worktree clean, pushed to origin.