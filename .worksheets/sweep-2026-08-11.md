# Commit Sweep: 2026-08-11

Commits reviewed: `03e3e0c..2285e89` (6 commits, go-on extension batch)

```
03e3e0c go-on extension: auto mode (shift+alt+enter burst, alt+shift+g toggle) + done-heuristics; gitignore web-search runtime state
ae39d13 fix go-on auto-mode races and completion detection
7a55cb4 harden go-on failure recovery and completion matching
d777b39 fix go-on stale watchdog on disarm
0fad716 fix go-on keybindings for legacy terminals
2285e89 fix go-on legacy burst key: ctrl+alt+g replaces unusable alt+enter
```

## Issues found

### Commit hygiene — clean
- Messages descriptive and conventional (`fix go-on ...`), no fixup/wip, no binaries, no secrets (`just ci` git-hygiene check green).

### Doc drift — clean
- `go-on.ts` header key-choices section kept in lockstep with every keybinding change; last commit (2285e89) rewrote it to reflect the ctrl+alt+g legacy burst and the alt+enter veto (pi-reserved `app.message.followUp` + GNOME Terminal/Windows Terminal interception).

### Test quality — Minor
- Behavioral tests (`/tmp/test-go-on.mjs`) are NOT tracked in the repo — they exist only in /tmp and were rebuilt per session. If /tmp is cleared, the go-on state machine loses its regression net. Consider committing a `pi/agent/extensions/go-on.test.mjs` (harness already has no pi imports beyond the extension itself).
- The 0fad716 test gap that caused the alt+enter bug: the harness records all `registerShortcut` calls without pi's reserved-key conflict filter, so a skipped binding looked registered. The 2285e89 test suite adds an explicit `!shortcuts.has("alt+enter")` regression assertion; a pi-side conflict simulation (like `/tmp/verify-go-on-keys.mjs`) is the only way to catch reserved-key skips.

### Cross-commit patterns — clean
- Single responsibility per commit; stop-heuristic state machine stayed in one file; no duplicated fixes.

### Architecture drift — clean
- No new dependencies; extension uses the documented pi API surface only (`registerShortcut/Command`, `on`, `sendUserMessage`, `ui`, `sessionManager`, `modelRegistry`).

## Notes
- Sequence-verification matrix (legacy + kitty) reproduced against installed pi dist in `/tmp/verify-go-on-keys.mjs`:
  - legacy `\x1b\x07` → `ctrl+alt+g` burst ✓ · `\x1bg` → `alt+g` ✓ · `\x1b\r` only matches `alt+enter` (reserved) ✗
  - kitty `\x1b[13;4u` → `alt+shift+enter` ✓ · `\x1b[103;4u` → `alt+shift+g` ✓ · `\x1b[103;7u` → `ctrl+alt+g` ✓ · `\x1b[103;3u` → `alt+g` ✓
- Independent review of the batch requested in parallel (go-on-review subagent); findings appended when it lands.
