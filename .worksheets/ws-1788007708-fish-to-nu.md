# Fish → Nushell cutover — 2026-09-24

## Task
Complete the fish → Nushell shell migration in this dotfiles repo so Nushell is
the default and canonical shell, with secrets consolidated onto systemd
`environment.d`.

## Human notes
<!-- decisions locked in via ask_user -->
- Default shell: **flip to Nushell** (bootstrap chsh -s nu).
- fish config: **keep as tracked fallback** — add a header noting Nushell is
  canonical; do not delete.
- Secrets: **consolidate to environment.d only** — remove `secrets/env.fish.*`
  and all `env.fish` references; `environment.d` becomes the single source for
  all shells.

## Todos
- [x] Flip `bootstrap.sh` to set Nushell as the default shell (chsh -s nu, cross-platform)
- [x] Add canonical-shell header to `config/fish/config.fish`
- [x] Migrate 17 env.fish-only keys into `secrets/environment.d.enc` (now 26 keys)
- [x] Remove `secrets/env.fish.enc` + `secrets/env.fish.sha256`
- [x] Add `secrets/environment.d.sha256` (plaintext-content sidecar, tracked)
- [x] Drop `env.fish` references: `post_deploy.bash`, `post_deploy.ps1`,
      `githooks/pre-commit`, `secrets/README.md`, `config/nushell/env.nu`
- [x] Repoint `config/fish/config.fish` to source `environment.d` (KEY=value)
- [x] Repoint `powershell/Load-Secrets.ps1` to parse `environment.d` (KEY=value)
- [x] Repoint `config/llama.cpp/sync-mcp-configs.py` to read `environment.d`
- [x] Update `README.md` (Nushell = default shell; secrets section)
- [ ] Brewfile: keep `fish` installed (decision) — no change needed
- [x] Sync live `~/.config/environment.d/99-environment.conf` to merged content
      (protects the pre-commit auto-encrypt from regressing to 9 keys)
- [x] Run `just ci` — green (4 secrets decryptable)
- [x] Commit and push (e19923a + b11b8b1 + a268cdf)
- [x] Fix Nushell to source environment.d directly (macOS has no systemd)
- [x] Verify 26/26 secrets load in both Nushell and Fish fallback
- [x] Remove stale `~/.config/secrets/env.fish` on this host (redundant; silences pre-commit warning)
- [x] Fix stale env.nu header comment

## Progress
- Discovered `environment.d` was NOT a superset of `env.fish`: 17 keys
  (CURSOR_API_KEY, JULES_API_KEY, OPENCODE_KEY, OLLAMA_API_KEY, …) lived only
  in env.fish. None were value-level duplicates of env.d keys.
- Merged losslessly: decrypted both, appended the 17 missing `KEY=value`
  lines to environment.d, re-encrypted with sops (local temp `.sops.yaml` +
  explicit age recipients; sops rejected `--age` alone due to config loading),
  verified a decrypt roundtrip, installed `secrets/environment.d.enc`, and
  wrote the plaintext-content `secrets/environment.d.sha256` sidecar.
- Pre-commit hazard identified & mitigated: the auto-encrypt hook would have
  re-encrypted the stale 9-key live file and overwritten the 26-key merge. Synced
  the live `~/.config/environment.d/99-environment.conf` to the merged content so
  its sha256 matches the sidecar → hook skips re-encryption.
- All `env.fish` runtime references removed from active configs/scripts.
  `FEEDBACK.md:380` left as a historical (past-tense) log entry.

## Findings
- sops `--encrypt` with `--age` still loads `.sops.yaml` and errors
  "no matching creation rules found" when the plaintext is outside `secrets/`.
  Workaround: a temp-dir local `.sops.yaml` with a catch-all rule.
- Sidecar convention (`.sha256`) stores the **plaintext** content hash, not the
  `.enc` hash (confirmed against `pi-auth.json.sha256`). `check-secrets` only
  verifies decryptability, so the convention is not enforced by CI.
- post_deploy.bash generic loop now skips every `.enc` (all handled by
  `app_secret`); harmless dead loop left in place.
- `bootstrap.sh` already installs Nushell via pacman (Linux) and Brewfile
  (macOS line 83); `fish` stays in Brewfile as the installed fallback.

## Decisions
- 2026-09-24: fish config kept as tracked fallback (not deleted) — lower risk.
- 2026-09-24: secrets consolidated to environment.d only; 17 env.fish keys
  migrated losslessly (both names kept for any renames; no data loss).
- 2026-09-24: live environment.d file synced to merged content pre-commit so the
  pre-commit auto-encrypt does not regress the merge.
- 2026-09-24: Brewfile keeps `fish` installed (fallback); only the default-shell
  choice changes.
- 2026-09-24: `.worksheets/` left untracked (local collaboration artifact).

## Cross-host verification
- framearch-juan (local): Nushell already installed (/usr/bin/nu); repo at
  HEAD 6478c9d; passwordless sudo → flipped login shell fish→nu via `sudo chsh`.
  28/28 secrets load in nu. DONE.
- arch-wsl: Arch WSL, passwordless sudo. Pulled (caught up), `pacman -S
  nushell` → /usr/bin/nu, chsh to nu. 28/28 load. DONE.
- mac-juan: Nushell installed via brew (0.115.1 at /opt/homebrew/bin/nu);
  environment.d deployed with 28 keys (git post-merge hook auto-deployed on
  pull); nu loads 28/28. Login shell still fish — `chsh -s` needs an interactive
  password (no passwordless sudo), so it's a manual 2-liner:
    echo /opt/homebrew/bin/nu | sudo tee -a /etc/shells
    chsh -s /opt/homebrew/bin/nu        # then log out/in
  (or run ~/dotfiles/bootstrap.sh on the Mac.)
- liedelpi (aarch64 Debian 13): repo DIVERGED — 357 local commits ahead / 656
  behind origin (real local work since 2026-06-17). Did an OUT-OF-BAND deploy
  without touching the 357 commits: installed prebuilt aarch64 Nushell 0.115.1
  to /usr/local/bin/nu, deployed env.nu from `origin/main` blob to
  ~/.config/nushell/env.nu, added to /etc/shells, chsh to nu. Copied the sops
  age private key from framearch-juan to ~/.config/sops/age/keys.txt (600), then
  decrypted the 28-key environment.d locally (liedelpi has no sops binary) and
  scp'd the plaintext to ~/.config/environment.d/99-environment.conf (600).
  nu now loads 28/28. DONE.
  ⚠️ Reconcile the divergent repo later and re-run dotter deploy to bring
  env.nu + secrets back under dotter management (and install sops on liedelpi
  so future post_deploy auto-decrypts).
- Backup: pushed `liedelpi-backup-20260829` (tip d8b3366, verified from
  framearch-juan) — 357 unpushed commits no longer single-copy.
- pwsh/Load-Secrets.ps1 syntax reviewed (correct) but not executed (no pwsh here).
