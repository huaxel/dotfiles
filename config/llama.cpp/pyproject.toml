[project]
name = "llama-cpp"
version = "0.1.0"
description = "Local llama.cpp model presets and setup helpers"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "huggingface-hub>=1.20.1",
]
