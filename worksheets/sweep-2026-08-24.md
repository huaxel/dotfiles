# Commit Sweep: 2026-08-24

Commits reviewed: `c3bfbd8..c07be3a` (pi-dynamic-footer / opencode-go-usage review follow-ups)

## Scope

Four review follow-up commits on `pi/packages/pi-dynamic-footer` and `pi/packages/opencode-go-usage`:

- `c3bfbd8` pi-dynamic-footer: fix provider SegmentKey drift + typecheck gate
- `6df55b2` pi-dynamic-footer: harden refresh, git polling, storage dir, and env wiring
- `8528ad3` opencode-go-usage: fix strict-tsc errors + typecheck gate
- `8a27faa` pi-dynamic-footer: bound the quota credential cache
- `c07be3a` docs(pi-dynamic-footer): note PI_CODING_AGENT_DIR override for storage dir

## Commit hygiene

- All messages conventional (`type(scope): description`), descriptive.
- No `fixup!`/`wip`/squash-me commits.
- No binaries or secrets; only `.ts`/`.json`/`.md` touched.

## Doc drift

- `6df55b2` changed storage to honor `PI_CODING_AGENT_DIR`; README's security section
  previously hardcoded `~/.pi/agent/observability/`. Fixed in `c07be3a`.
- No other behavior changes missing docs.

## Test quality

- Each code change has coverage:
  - `c3bfbd8` — typecheck gate added; `formatResetTime` flake stabilized + verified 6×.
  - `6df55b2` — `getAgentDir`/`getDefaultObservabilityDir` test added.
  - `8528ad3` — typecheck script added to both packages.
  - `8a27faa` — quota cache cap test added (80-key rotation, mocked fetch).
- No skipped tests, no empty/trivial tests.
- Gates: footer 44/44, dependency 21/21, `just ci` green.

## Cross-commit patterns

- Consistent: single source of truth for agent dir now lives in `lib/storage/index.ts`
  (footer) and `lib/auth/auth.ts` (dependency); the two packages share the same
  `PI_CODING_AGENT_DIR` semantics.
- No repeated bug-fixes; each issue fixed once at the root.

## Architecture drift

- No new dependencies.
- No layering violations; footer still imports quota-provider + dependency only.
- `quotaCacheSize()` export is a read-only test support surface, consistent with the
  module's existing exported helpers (`clampPercent`, `safeError`, etc.).

## Action items

- None blocking.

## Clean bill? (yes / no — if no, list what's needed before ship)

Yes for the reviewed range.

---

## Current session: Ghostty-synced Pi theme

Reviewed the pending `pi-ghostty-theme-sync` theme hierarchy changes:

- Explicit synchronized foreground for user/custom messages, search matches, and code blocks.
- Accent-colored tool titles with muted tool output.
- Generator, checked-in theme, README, and package metadata updated together.
- JSON validation and `just ci` passed; ShellCheck and Deno checks were skipped because those tools are not installed.

No blocking issues found.

Second pass: audited secondary output against tinted surfaces. Added a dedicated blended `toolOutput` color (4.57:1–5.78:1 on active search/success/error surfaces) and slightly raised `dim` for URL readability. Gate remained green.

Third pass: extracted `theme.ts` from the Pi runtime wrapper and added three Node regression tests covering config parsing, color normalization, token completeness, and hierarchy. `npm test`, `npm pack --dry-run`, and `just ci` passed.

Fourth pass: added WCAG contrast-ratio checks that minimally nudge low-contrast ANSI semantic colors toward black or white on alternate light/dark palettes. Four package tests pass, and the active Tokyo Night snapshot remains byte-for-byte reproducible.

Pi update follow-up: global Pi is now 0.84.3; built-in 0.84.3 themes still use the current token schema. The custom package tests and active snapshot comparison pass. The generated package cache reports six dependency vulnerabilities, but `npm audit fix --dry-run` found no non-breaking fixes, so no forced remediation was applied. Install-script warnings are expected from the configured `--ignore-scripts` policy.

Prompt-background follow-up: `userMessageBg` was only a small brightness shift (`#22232e` vs `#1a1b26`, roughly 1.1:1). Changed it to a foreground blend (`#2e303f`, roughly 1.36:1) and added a regression assertion. Tests, snapshot comparison, and `just ci` pass.

Live-editor follow-up: Pi 0.84.3's supported `CustomEditor`/`setEditorComponent` path now decorates the live editor at render time with `userMessageBg` and `borderAccent`. The subclass does not override `handleInput`, so Pi retains normal keybindings, app actions, autocomplete, and paste handling. It skips installation when another custom editor is already configured. Added pure ANSI reset regression coverage and documented the package behavior; package `npm test` (6/6), syntax checks, `npm pack --dry-run`, and `just ci` passed. ShellCheck and Deno remain unavailable locally.

Review notes: no blocking issues found. The independent review skill referenced by AGENTS.md remains unavailable at `~/.agents/skills/requesting-code-review/SKILL.md`, so this isolated change received a manual diff/API review instead.

Follow-up runtime validation: a real pseudo-terminal launch rendered typed input with the expected background and accent border; typing `/` produced autocomplete output with the decoration still active. No code changes were needed.

User correction: the stronger surface was intended for sent chat messages only, not the live prompt zone. Reverted the custom editor wrapper and retained the transcript `userMessageBg` change.
