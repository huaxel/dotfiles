# Commit Sweep: 2026-08-27

Commits reviewed: `9accd75^..7139a1b`

## Issues found

- `9accd75` — built the Openference quota feature against `GET https://api.openference.com/v1/usage`, which does not exist. The earlier 401 (invalid key) and 402 (quota-exhausted) responses were auth+quota middleware short-circuiting before routing; a valid key with available quota returns 404. Fixed in `7139a1b` by retargeting the real dashboard endpoint `GET https://openference.com/api/user/me` and mirroring the UI's cost-weighted field precedence.

## Action items

- [x] `7139a1b` — corrected endpoint, parser, tests, READMEs; verified live.

## Clean bill? (yes / no — if no, list what's needed before ship)

Yes after `7139a1b`. The Openference feature now targets the real, key-authenticated endpoint and matches the dashboard's quota math. The working tree still contains unrelated pre-existing modifications in `config/herdr/config.toml` and `pi/agent/settings.json`; they were intentionally not included.
