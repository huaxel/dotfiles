# ─────────────────────────────────────────────
# Pi agent — transient / runtime data
# ─────────────────────────────────────────────

# Runtime state
sessions/
git/
intercom/
observability/
tmp/

# Managed binaries (auto-downloaded by pi)
bin/

# Logs
pi-crash.log
run-history.jsonl
pi-debug.log

# Model cache (auto-generated)
models-store.json

# Provider catalog caches / runtime state (auto-generated by pi)
commandcode-models.json
cursor-sdk-model-list.json
opencode-go-failover-state.json

# web_search tool runtime state (fetch cache + tool config, auto-managed)
web-search-cache/
web-search.json

# Editor / OS junk
.DS_Store
*.bak
*.bak-recovery-*

# Secrets — auth.json has API keys; quota-sessions.json has web session cookies
auth.json
quota-sessions.json
*.pi-fff.json
