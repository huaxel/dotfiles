# pi-commandcode-provider@0.4.2 peer-requires @earendil-works/pi-coding-agent@^0.75.5,
# but the installed pi-coding-agent is 0.83.x (required by most other extensions).
# No newer commandcode-provider release exists, so resolve peers leniently.
# See: https://docs.npmjs.com/cli/v10/using-npm/config#legacy-peer-deps
legacy-peer-deps=true
