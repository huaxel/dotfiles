diff --git a/node_modules/pi-autoresearch/skills/autoresearch-create/SKILL.md b/node_modules/pi-autoresearch/skills/autoresearch-create/SKILL.md
index 4a9eb34..221be3d 100644
--- a/node_modules/pi-autoresearch/skills/autoresearch-create/SKILL.md
+++ b/node_modules/pi-autoresearch/skills/autoresearch-create/SKILL.md
@@ -134,20 +134,30 @@ pnpm test --run --reporter=dot 2>&1 | tail -50
 pnpm typecheck 2>&1 | grep -i error || true
 ```
 
-## Loop Rules
-
-**LOOP FOREVER.** Never ask "should I continue?" — the user expects autonomous work.
-
-- **Primary metric is king.** Improved → `keep`. Worse/equal → `discard`. Secondary metrics rarely affect this.
-- **Annotate every run with `asi`.** Record what you learned — not what you did. What would help the next iteration or a fresh agent resuming this session?
-- **Watch the confidence score.** After 3+ runs, `log_experiment` reports a confidence score (best improvement as a multiple of the session noise floor). ≥2.0× means the improvement is likely real. <1.0× means it's within noise — consider re-running to confirm before keeping. The score is advisory — it never auto-discards.
-- **Simpler is better.** Removing code for equal perf = keep. Ugly complexity for tiny gain = probably discard.
-- **Don't thrash.** Repeatedly reverting the same idea? Try something structurally different.
-- **Crashes:** fix if trivial, otherwise log and move on. Don't over-invest.
-- **Think longer when stuck.** Re-read source files, study the profiling data, reason about what the CPU is actually doing. The best ideas come from deep understanding, not from trying random variations.
-- **Resuming:** if `.auto/prompt.md` exists, read it + git log, continue looping.
-
-**NEVER STOP.** The user may be away for hours. Keep going until interrupted.
+## Loop Rules (with a safety cap)
+
+**Run up to the configured cap, then stop and report.** Autoresearch can burn
+hours of compute autonomously; the model judges the metric and also decides
+whether improvements are real, so unbounded loops risk rationalizing wasted
+runs. Set `maxIterations` in `.auto/config.json` (see below) and respect it:
+
+- **Stop at the cap.** When `maxIterations` is reached, log the final state,
+  summarize wins/losses, and report to the user. Do not silently continue.
+- **No cap set? Stop after ~15-20 runs** (or one hour of looping) and report
+  findings to the user, proposing next steps rather than continuing.
+- If a run is in flight when the cap hits, finish that one cycle, then stop.
+- Respect a user's "stop now" at the next iteration boundary — never argue.
+
+Otherwise: **primary metric is king.** Improved → `keep`. Worse/equal →
+`discard`. Secondary metrics rarely affect this. **Annotate every run with
+`asi`** — record what you learned, not what you did, so a fresh agent resuming
+can avoid dead ends. Watch the confidence score (≥2.0× likely real, <1.0×
+within noise — advisory only, never auto-discards). "Simpler is better" —
+removing code for equal perf = keep; ugly complexity for a tiny gain =
+discard. Don't thrash; if repeatedly reverting the same idea, try something
+structurally different. Crashes: fix if trivial, else log and move on. When
+stuck, re-read source files and the profiling data rather than trying random
+variations. On resume, read `.auto/prompt.md` + git log and continue.
 
 ## Ideas Backlog
 
