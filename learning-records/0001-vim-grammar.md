# LR-0001: Vim grammar — operators, motions, text objects, counts (demonstrated)

User aced the Lesson 1 quiz (5/5): `ciw` mid-word word-replace (avoiding the `cw` trap), the `di(` vs `da(` delimiter distinction, `gUiw` whole-word uppercase, `d%` block delete, and `3dd` line counting — including grasping that counts attach to either operator (`3dd`) or motion (`d2j`) with the same result.

Evidence: quiz answers 1b 2a 3b 4b 5a, plus hands-on practice in nvim.

Implications: the operator+noun model is now a working mental model, not memorized keys. Lesson 2 (the `.` command and registers) can build directly on it, and mini.ai's extended objects and visual mode can be layered later without re-teaching basics. This sets the floor for all future sessions.
