# LR-0004: Macros (demonstrated) — core grammar arc complete

User aced the Lesson 4 quiz (5/5): recording into a register (`qa`), replay (`@a`), the advance step (`j`) as macro hygiene for line-by-line replay, counts (`5@a`), and mass application (`:%normal @a`).

Evidence: quiz answers 1a 2a 3a 4a 5a.

Implications: the core-grammar arc is complete — operators/motions/text objects (LR-0001), dot command + registers (LR-0002), visual mode + treesitter objects (LR-0003), macros (LR-0004). The user now has a working vim mental model. Mission pivot: sessions now move to the "understand my nvim" half — the actual keymap map (which-key), the LSP/format/test workflow, and git, all grounded in the user's installed LazyVim v16 + custom plugin config rather than assumptions.
